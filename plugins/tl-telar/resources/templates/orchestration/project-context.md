# Project Context (Maintained by Orchestrator)

<!-- This file is the orchestrator's working scratchpad. It is NOT committed to git (per .tl-telar hygiene §2.7a). It IS read by recovery and by every subsequent WU's implementer so the orchestrator builds up a coherent picture across the plan. -->

## Tooling

- Package manager: {{npm|pnpm|yarn|bun|pub}}
- Test runner: {{jest|vitest|flutter test|none}} ({{config-file-path}})
- Linter: {{eslint|flutter analyze|none}} ({{config-file-path}})
- Build: {{vite|metro|flutter build|other}}
- Framework: {{react-native|expo|flutter|native}}

## Completed Work Units

| WU | Title | Key Files | Services/Modules Created |
|----|-------|-----------|--------------------------|

## Established Patterns

<!-- Patterns the orchestrator has observed during execution. Sub-spec 5 will surface KB facts here via /tl-telar:prime. -->

## Active Services

See SERVICE-INVENTORY.md if present, else this section is the running list.
